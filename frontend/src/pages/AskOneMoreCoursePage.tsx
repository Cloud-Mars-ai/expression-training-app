import { ArrowRight, CheckCircle2, MessageCircleQuestion, XCircle } from "lucide-react";
import { Link } from "react-router-dom";
import { researchMethods } from "../features/structured-expression/content";

export function AskOneMoreCoursePage() {
  const method = researchMethods.find((item) => item.method_id === "M12");
  return <div className="mx-auto w-full max-w-[720px] pb-8"><section className="border-b border-line pb-6"><div className="flex items-center gap-2 text-primary-strong"><MessageCircleQuestion size={20} /><p className="micro-label">MICRO COURSE · 多问一句</p></div><h2 className="mt-3 text-2xl font-bold">先确认理解，再推动下一步</h2><p className="mt-3 text-sm leading-6 text-ink-soft">适合会议、协作和校园沟通。目标不是重复对方原话，而是减少误解并形成可执行下一步。</p></section><section className="py-6"><p className="micro-label text-ink-muted">THREE MOVES · 三个动作</p><div className="mt-4 grid gap-3 sm:grid-cols-3">{[["1", "复述目标", "用自己的话说出对方真正想完成什么。"], ["2", "确认理解", "补一个具体问题，确认范围、优先级或标准。"], ["3", "给下一步", "说清谁在何时做什么，以及如何回报进度。"]].map(([step, title, body]) => <div className="rounded-lg border border-line bg-surface p-4" key={step}><span className="level-badge">{step}</span><h3 className="mt-3 font-bold">{title}</h3><p className="mt-2 text-sm leading-6 text-ink-soft">{body}</p></div>)}</div></section><section className="grid gap-4 sm:grid-cols-2"><div className="rounded-lg border border-danger/30 bg-danger-soft p-4"><div className="flex items-center gap-2 text-danger"><XCircle size={18} /><h3 className="font-bold">低质量回应</h3></div><blockquote className="mt-3 text-sm font-semibold leading-6">“好的，我知道了，我会尽快处理。”</blockquote><p className="mt-3 text-xs leading-5 text-ink-soft">问题：没有确认目标和完成标准，“尽快”也没有具体时间。</p></div><div className="rounded-lg border border-success/30 bg-success-soft p-4"><div className="flex items-center gap-2 text-success"><CheckCircle2 size={18} /><h3 className="font-bold">高质量回应</h3></div><blockquote className="mt-3 text-sm font-semibold leading-6">“你的目标是明天下午前拿到可讨论的初稿，对吗？我先完成核心结构，今天六点前发你确认方向。”</blockquote><p className="mt-3 text-xs leading-5 text-ink-soft">技巧：复述目标、补一个确认问题、给出明确下一步。</p></div></section>{method && <section className="mt-6 border-y border-line py-5"><p className="micro-label text-ink-muted">RESEARCH METHOD · {method.method_id}</p><h3 className="mt-2 font-bold">{method.method_name}</h3><p className="mt-2 text-sm leading-6 text-ink-soft">{method.evidence_basis}</p><p className="mt-3 text-xs leading-5 text-ink-muted">成功信号：{method.success_signal}</p></section>}<Link className="primary-button mt-6 w-full" to="/training/scenario">选择真实场景开始训练 <ArrowRight size={17} /></Link></div>;
}
