# Attempts module boundary

Agent 2 owns this directory after the contracts freeze. The module must import shared API and domain types from `@expression-training/contracts` and must not redefine them.

