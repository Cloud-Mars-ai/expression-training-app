# 表达能力训练 APP · 试用版 1

## 直接体验

1. 双击 `启动试用版.bat`。
2. 浏览器会自动打开首页。
3. 体验期间请保持命令窗口开启。
4. 使用结束后，在命令窗口按 `Ctrl+C` 停止本地服务。

运行网页成品不需要安装 Node.js，启动器使用 Windows 自带的 PowerShell 和 .NET。

如果 4173 端口已被占用，启动器会自动尝试 4174 至 4189 端口。

## 目录说明

- `app/`：已经构建完成的网页成品。
- `frontend/`：React + TypeScript + Vite 前端源码，可用于继续开发。
- `contracts/`：前后端共享的 Attempt、Transcript、Evaluation 和 API 契约。
- `server/`：后端模块目录；当前共同基础阶段尚未实现业务接口。
- `local-server.ps1`：基于 Windows 自带组件的本地静态服务器，支持 React Router 刷新回退。
- `启动试用版.bat`：Windows 双击启动入口。

## 继续开发

只有继续开发源码时才需要 Node.js。在 PowerShell 中进入 `frontend` 目录，然后运行：

```powershell
npm install
npm run dev -- --host 127.0.0.1
```

生产构建命令：

```powershell
npm run build
```

## 演示数据

训练记录、结果、筛选、复习项目和隐私偏好保存在当前浏览器的 localStorage 中。可以在“我的”页面通过二次确认重置演示数据。

## 全网蒸馏训练题库

- “训练 → 结构化表达”会显示 80 道训练题，可按生活化讨论、经典辩论、社会讨论、校园与个人成长、求职与初入职场、写作方法迁移筛选。
- 运行数据来自相邻目录 `07-全网蒸馏研究/topics.json`，CSV 文件继续作为审计与导出来源。
- 研究数据库更新后，在项目根目录运行 `npm run sync:topics`。脚本会检查题目总数、唯一编号、必填字段和时间配置，再生成前后端共用快照。

## 主题切换

- 页面右上角的调色盘按钮可快速切换“白色浅色”和“淡黄莫兰迪”主题。
- “我的 → 主题风格”也可以选择主题。
- 首页、准备、录音、转写校对和结果页会使用同一主题，不再单独切换到黑色作答页。

## 真实语音识别与 DeepSeek 分析

运行版不再用固定示例文字冒充录音内容。没有配置识别服务时，本次练习会明确标记为技术失败，不生成转写或低分。

原始录音只交给本机语音识别服务，不会发送给 DeepSeek。用户在转写校对页确认后，服务端只把确认后的分段文字、当前题目和评分标准发送到 `https://api.deepseek.com`。

1. 复制 `.env.example` 为 `.env.local`。
2. 启动兼容 OpenAI `/audio/transcriptions` 的本机 Whisper、Faster-Whisper 或 whisper.cpp 服务。
3. 在 DeepSeek 开放平台创建 API Key。
4. 打开 `.env.local`，取消 `DEEPSEEK_API_KEY=` 的注释并在等号后填写密钥。不要把密钥提交到 Git 或粘贴到前端代码。
5. 保持 `EVALUATION_PROVIDER=deepseek`、`DEEPSEEK_BASE_URL=https://api.deepseek.com` 和 `DEEPSEEK_MODEL=deepseek-chat`。
6. 重新双击 `启动试用版.bat`。

填写密钥后，可以先在项目根目录运行 `npm run verify:deepseek`。该命令只发送一段明确标注为连通性测试的合成文字，不读取录音或历史作答，也不会输出 API Key。

DeepSeek 分析只使用用户确认后的转写、题目和评分标准，并把证据绑定到真实转写片段。鉴权、网络、限流、超时或返回结构异常都会标记为技术失败，不会计入成长进度。

注意：DeepSeek 网页版不能直接当作程序 API 使用，必须使用开放平台 API Key。API 调用可能产生服务商费用，并受 DeepSeek 账户的数据处理与保留政策约束。
